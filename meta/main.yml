galaxy_info:
  author: Henryk Konsek
  description: Ensures that OpenShift is installed and running as a systemd service. Also installs `oc` and `oadm` clients.
  company: Red Hat

  license: Apache 2.0

  min_ansible_version: 1.2

  platforms:
  - name: Fedora
    versions:
    - 26

  galaxy_tags: ["fedora", "docker", "kubernetes", "openshift"]